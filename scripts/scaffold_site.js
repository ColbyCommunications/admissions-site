// do we have files?
    // yes: we're processing an existing repo
    // no: we're building new site
// build site_specific folder
    // build out lando folder
    // build out platform folder
    // build empty test folder
    // generate composer_requirements.json
    // generate robots.txt
// if new site: generate files from starter
// if existing repo: generate file edits

